@page
@model Zoologicos_Presentacion.Pages.Ventanas.ZoologicosModel

@{
    ViewData["Title"] = "Sedes de Zoológicos";
}

<div class="text-center">
    <form method="POST" enctype="multipart/form-data">
        @Html.AntiForgeryToken()

        <h1 class="display-4">
            Administración de Sedes de Zoológicos
        </h1>

        <div class="bt_panel_buttons">
            <input type="submit" value="Refrescar" asp-page-handler="BtRefrescar" />
            <input type="submit" value="Nuevo" asp-page-handler="BtNuevo" />
            <input type="submit" value="Guardar" asp-page-handler="BtGuardar" />
        </div>

        @if (Model.Zoologico != null)
        {
            <div class="row justify-content-center text-left" style="text-align: left; margin-bottom: 20px;">
                <div class="col-lg-6">
                    
                    <div class="form-group">
                        <label>Nombre del Zoológico</label>
                        <input id="inNombre" class="cts_forms form-control" asp-for="@Model.Zoologico!.Nombre" placeholder="Ej: Zoológico de Cali, Bioparque Guátika" required />
                    </div>

                    <div class="form-group">
                        <label>Ubicación / Ciudad / Dirección</label>
                        <input id="inUbicación" class="cts_forms form-control" asp-for="@Model.Zoologico!.Ubicacion" placeholder="Ej: Cali, Valle del Cauca o Cra 44 #..." required />
                    </div>
                    
                </div>
            </div>
        }

        @if (ViewData.ContainsKey("Mensaje"))
        {
            @await Html.PartialAsync("../Emergentes/Mensajes.cshtml")
            ;
        }
        
        @if (@Model.Zoologico != null && Model.Borrando)
        {
            <div class='popup'>
                <div class='pp_ms_inner'>
                    <div style='width:95%'>
                        <input type="hidden" asp-for="@Model.Zoologico!.Id" /> 

                        <h2 align="left">Mensaje</h2>
                        <h4 align="left">¿Desea eliminar de forma permanente esta sede?</h4>
                        <h6 align="left">
                            <strong>Sede:</strong> @Model.Zoologico!.Nombre <br />
                            <strong>Ubicación:</strong> @Model.Zoologico!.Ubicacion
                        </h6>

                        <input type="submit" value="Cerrar" asp-page-handler="BtCerrar" class="btn btn-secondary" />
                        <input type="submit" value="Borrar" asp-page-handler="BtBorrar" class="btn btn-danger" />
                    </div>
                </div>
            </div>
        }

        @if (Model != null && Model.Lista != null && Model.Lista.Count > 0)
        {
            <table class="tb_data table table-striped table-bordered table-hover">
                <thead>
                    <tr>
                        <th>ID Sede</th>
                        <th>Nombre de la Sede</th>
                        <th>Ubicación Geográfica</th>
                        <th class="cl_actions">Acción</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var elemento in Model.Lista)
                    {
                        <tr>
                            <td>🏢 Sede #@elemento.Id</td>
                            <td><strong>@elemento.Nombre</strong></td>
                            <td>📍 @elemento.Ubicacion</td>
                            <td>
                                <input type="submit" value="Modificar" asp-page-handler="BtModificar" class="btn btn-secondary btn-sm" asp-route-data="@elemento.Id" />
                                <input type="submit" value="Borrar" asp-page-handler="BtBorrarVal" class="btn btn-secondary btn-sm" asp-route-data="@elemento.Id" />
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
    </form>
</div>